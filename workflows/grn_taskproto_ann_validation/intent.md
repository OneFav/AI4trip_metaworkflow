# Intent

基于提供的 GRN 候选或 GRN 模块，自动完成以下目标：

- 将 GRN 与 ANN 统一转成可比较的 Probe-Induced Response Geometry（PIRG）
- 在固定任务原型库内完成任务原型匹配与 Top-K 假设输出
- 对候选 ANN 进行统一测量与距离排序
- 通过外部实验验证“距离更近 → 更适合该任务原型”的假设可靠性

边界约束：

- 不做开放世界的“GRN→任意任务”生成
- 只在固定任务原型库内排序、筛选、参数化
- 所有结论必须经过外部验证
