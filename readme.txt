请基于当前项目做一轮“最小侵入式”的结构优化，目标不是重构系统，而是在尽量不破坏现有架构、接口、目录组织和运行方式的前提下，让以下三类内容在逻辑上更清晰、后续更容易迭代：

1. 整体任务目标与全局约束；
2. 各 checkpoint / skills 的局部执行约束；
3. 对执行结果的评估规则。

请先完整理解当前代码与文件组织，优先复用已有结构，而不是引入新的复杂抽象。只有在现有表达明显混杂、导致后续难以维护时，才做必要的轻量拆分或增补。重点是“解耦语义”，不是“重写工程”。

修改原则：
- 保持现有主流程可运行，避免大规模重命名、迁移和接口破坏；
- 保持向后兼容，已有功能和已有数据尽量继续可用；
- 不为了概念完整而过度设计，不引入不必要的新层级、新框架或新依赖；
- 优先做局部整理、轻量抽象、字段补充、职责澄清，而不是整体重构；
- 如果某些概念可以先通过注释、配置结构或轻量协议表达清楚，就不要急着做复杂代码化。

希望你重点关注：
- 当前“任务目标 / skill 约束 / 评估规则”是否混在同一处，若混杂，请做适度分离；
- 当前评估是否只有最终结果，或只有过程检查；请在不打乱现有流程的前提下，让系统更容易同时支持“最终结果判断”和“关键 checkpoint 的辅助判断”；
- 当前是否缺少对运行过程、评估结论、修改决策的基本记录；若缺少，请以最轻量的方式补足，便于后续迭代 skills；
- skill 的边界尽量与可观察的 checkpoint 对齐，避免把流程拆得过碎。

工作方式要求：
1. 先阅读和分析当前项目，判断最小必要改动集；
2. 先给出简洁的修改方案，说明哪些地方保留、哪些地方只做轻量调整；
3. 再开始实施，分步修改，避免一次性大改；
4. 每一步都说明该改动为什么是必要且最小的；
5. 最后给出结果总结，包括：
   - 本次做了哪些最小改动；
   - 哪些旧结构被保留；
   - 现在系统在逻辑上如何区分目标、skills、评估；
   - 还有哪些问题暂时不改，以避免破坏当前结构。

请始终以“保守、可运行、可回退、可继续迭代”为优先级。